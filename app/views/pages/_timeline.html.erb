<%= inline_annotated_timeline @page.timeline(params[:date]), 620, 280, 'div_id_to_create', 
    {:zoomEndTime => @page.sorted_dates(params[:date])[params-1], :zoomStartTime  => @page.sorted_dates(params[:date])[-30] } %>
